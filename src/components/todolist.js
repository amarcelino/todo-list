import React from 'react'

const TodoList = () => (
  <div>
    <h1>Todo List</h1>
    <input type='text' placeholder='Add To do' />
    <button>Add </button>

    <ul />
  </div>
)

export default TodoList
